% Modele : tableau - extension de plain avec tableau lu depuis un fichier CSV
% Champs optionnels : title_tex, table_tex, table_num_tex, background_block, section_tex, slide_label, section_label
\begin{frame}[t]
<%- background_block %>
\vspace{0.6cm}
{\fontsize{19}{19}\selectfont\color{NoumanityBlack}\raggedright <% if slide_label ~= "" then %><%- slide_label %>\hspace{0.5em}<% end %><%- title_tex %>\par}
\vspace{0.4em}
<% if table_num_tex ~= "" then %>{\footnotesize\color{NoumanityBlack!60} Tableau~<%- table_num_tex %>}\par\vspace{0.3em}<% end %>
<%- table_tex %>
<% if content ~= "" then %>
\vspace{0.5em}
<%- content %>
<% end %>
<% if section_tex and section_tex ~= "" then %>
\begin{tikzpicture}[remember picture, overlay]
  \node[anchor=south east, inner sep=0pt, xshift=-1.4cm, yshift=0.35cm]
    at (current page.south east) {%
      {\footnotesize\color{NoumanityBlack!50} <% if section_label ~= "" then %><%- section_label %>\hspace{0.5em}<% end %><%- section_tex %>}};
\end{tikzpicture}
<% end %>
\end{frame}
